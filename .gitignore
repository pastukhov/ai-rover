# PlatformIO build/system
.pio/
.pio-core/

# Local library mirrors (reference-only, not tracked)
libraries/

# Local secrets (do not commit)
include/secrets.h

# Logs and local runtime artifacts
*.log
log/
sessions/
history.jsonl
auth.json

# ESP-IDF managed components (auto-downloaded)
managed_components/

# Figma capture HTML (throwaway)
docs/figma/

# Environment files
.env
.env.*
